# Supabase — הוראות הפעלה (חד־פעמי)

החיבור לסופאבייס אופציונלי: בלי משתני הסביבה האפליקציה עובדת מקומית
בדיוק כמו קודם (localStorage + alerts.json). עם המשתנים — מתווספים
התחברות, סנכרון בין מכשירים והתראות קבועות.

## 1. יצירת פרויקט (חינמי)

1. להיכנס ל־https://supabase.com/dashboard וליצור פרויקט חדש (Free plan).
2. לחכות שהפרויקט יעלה (דקה־שתיים).

## 2. הרצת הסכמה (פעם אחת)

1. בדשבורד: **SQL Editor** → **New query**.
2. להדביק את כל תוכן הקובץ `supabase_schema.sql` מהריפו → **Run**.
3. מוודאים שהטבלאות נוצרו: alerts, drawings, chart_layouts, watchlists,
   custom_indicators (בטאב Table Editor).

## 3. הפעלת התחברות במייל (magic link)

1. בדשבורד: **Authentication** → **Sign In / Up** → לוודא ש־**Email** מאופשר.
2. **Authentication** → **URL Configuration** → להוסיף ב־**Redirect URLs**:
   `https://<כתובת־האתר־שלך>/` (למשל `https://elroi-terminal.onrender.com/`)
   וגם `http://localhost:8000/` לבדיקות מקומיות.

## 4. משתני סביבה ברנדר

בדשבורד של Render → השירות `elroi-terminal` → **Environment** → להוסיף:

| משתנה | ערך | איפה לוקחים |
|---|---|---|
| `SUPABASE_URL` | `https://xyzcompany.supabase.co` | Supabase → **Project Settings** → **Data API** |
| `SUPABASE_ANON_KEY` | המפתח הציבורי (anon) | אותו מסך |
| `SUPABASE_SERVICE_KEY` | מפתח ה־service_role (**סודי!**) | אותו מסך |

אחרי הוספת המשתנים — **Manual Deploy** → **Deploy latest commit**.
לא לחשוף את ה־SERVICE KEY בשיחה או בקוד — רק במשתני הסביבה של רנדר.

## 5. בדיקה

1. לפתוח את האתר — אמור להופיע חלון "☁ סנכרון ענן".
2. להזין אימייל → "שלח קישור התחברות" → ללחוץ על הקישור במייל.
3. אחרי ההתחברות: כפתור 👤✓ בסרגל העליון, והודעת "☁ הסנכרון הושלם".
4. לצייר קו על הגרף → לחכות ~2 שניות → לבדוק בטאב Table Editor
   שהופיעה שורה בטבלת `drawings`.

## 6. איך נמנעת ההשהיה של הפרויקט החינמי

פרויקט חינמי בסופאבייס מושהה אחרי ~7 ימים ללא פעילות. כדי למנוע זאת,
לולאת ההתראות של השרת (כל 5 דקות) מבצעת שאילתת heartbeat זולה
(`SELECT id ... LIMIT 1` על טבלת alerts) גם כשאין התראות פעילות —
כך הפרויקט תמיד נחשב פעיל, באפס עלות.

שימו לב: זה שומר על **סופאבייס** ער. השרת עצמו ברנדר החינמי עדיין
נרדם אחרי 15 דקות ללא תנועה (keep-alive קיים מטפל בזה) — אם השרת
ישן, גם ה־heartbeat לא רץ. לשירות קריטי 24/7 יש לשקול תוכנית בתשלום.
